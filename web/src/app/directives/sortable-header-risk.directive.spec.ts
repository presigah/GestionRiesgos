import { SortableHeaderRiskDirective } from './sortable-header-risk.directive';

describe('SortableHeaderRiskDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableHeaderRiskDirective();
    expect(directive).toBeTruthy();
  });
});
