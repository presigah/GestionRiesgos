import { SortableHeaderProjectDirective } from './sortable-header-project.directive';

describe('SortableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new SortableHeaderProjectDirective();
    expect(directive).toBeTruthy();
  });
});
